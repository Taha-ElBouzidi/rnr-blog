<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background: #4F46E5; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
      .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; }
      .comment { background: white; padding: 15px; margin: 15px 0; border-left: 4px solid #4F46E5; }
      .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 12px; }
      .btn { display: inline-block; background: #4F46E5; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; margin: 10px 0; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1 style="margin: 0;">💬 New Comment</h1>
      </div>
      
      <div class="content">
        <p>Hi <%= @recipient.name %>,</p>
        
        <p>
          <strong><%= @commenter.name %></strong> commented on the post 
          <strong>"<%= @post.title %>"</strong>:
        </p>
        
        <div class="comment">
          <%= simple_format(@comment.body) %>
        </div>
        
        <p>
          <%= link_to "View full conversation", post_url(@post), class: "btn" %>
        </p>
      </div>
      
      <div class="footer">
        <p>You're receiving this because you're <%= @recipient.id == @post.user_id ? "the author of" : "following" %> this post.</p>
      </div>
    </div>
  </body>
</html>
