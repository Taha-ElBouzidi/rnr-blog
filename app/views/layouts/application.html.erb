<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "First" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="First">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <header class="bg-white shadow sticky top-0 z-50">
      <nav class="max-w-full px-4 py-2 flex items-center justify-between">
        <h1 class="text-2xl font-bold text-blue-600">
          <%= link_to "Blog", posts_path, class: "text-blue-600 no-underline" %>
        </h1>
        <div id="user_nav">
          <% if user_signed_in? %>
            <div class="flex items-center gap-3">
              <%= link_to "Home", posts_path, class: "btn btn-secondary" %>
              <%= link_to "📚 Menu", menu_path, class: "btn btn-secondary" %>
              <% if current_user.admin? %>
                <%= link_to "⚡ Admin", admin_root_path, class: "btn btn-primary" %>
              <% end %>
              <%= link_to "⚙️ Account", edit_account_path, class: "btn btn-secondary" %>
              <span class="text-gray-900 text-sm font-semibold"><%= current_user.name %></span>
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger", data: { turbo: false } %>
            </div>
          <% else %>
            <div class="flex items-center gap-3">
              <%= link_to "📚 Menu", menu_path, class: "btn btn-secondary" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
              <%= link_to "Login", new_user_session_path, class: "btn btn-secondary" %>
            </div>
          <% end %>
        </div>
      </nav>
    </header>

    <div id="flash_messages">
      <%= render "shared/flash" if flash.any? %>
    </div>

    <main class="py-4">
      <%= yield %>
    </main>
    
  </body>
</html>
