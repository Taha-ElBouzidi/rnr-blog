<!DOCTYPE html>
<html>
  <head>
    <title>Admin Dashboard - <%= content_for(:title) || "First" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <div class="flex h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-gray-900 text-white flex flex-col">
        <div class="p-4 border-b border-gray-700">
          <h1 class="text-xl font-bold">⚡ Admin Panel</h1>
          <p class="text-xs text-gray-400 mt-1"><%= current_user.email %></p>
        </div>
        
        <nav class="flex-1 p-4 space-y-1">
          <%= link_to admin_root_path, class: "block px-4 py-2 rounded hover:bg-gray-800 #{current_page?(admin_root_path) ? 'bg-gray-800' : ''}" do %>
            📊 Dashboard
          <% end %>
          
          <%= link_to admin_users_path, class: "block px-4 py-2 rounded hover:bg-gray-800 #{current_page?(admin_users_path) ? 'bg-gray-800' : ''}" do %>
            👥 Users
          <% end %>
          
          <%= link_to admin_posts_path, class: "block px-4 py-2 rounded hover:bg-gray-800 #{current_page?(admin_posts_path) ? 'bg-gray-800' : ''}" do %>
            📝 Posts
          <% end %>
          
          <%= link_to admin_comments_path, class: "block px-4 py-2 rounded hover:bg-gray-800 #{current_page?(admin_comments_path) ? 'bg-gray-800' : ''}" do %>
            💬 Comments
          <% end %>
          
          <div class="border-t border-gray-700 my-4"></div>
          
          <%= link_to root_path, class: "block px-4 py-2 rounded hover:bg-gray-800" do %>
            🏠 Back to Site
          <% end %>
        </nav>
      </aside>

      <!-- Main Content -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top Bar -->
        <header class="bg-white shadow-sm px-6 py-4 flex items-center justify-between">
          <h2 class="text-2xl font-bold text-gray-800"><%= yield :page_title %></h2>
          <div class="flex items-center gap-3">
            <span class="text-sm text-gray-600"><%= current_user.name %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, 
              class: "btn btn-sm btn-danger",
              form: { data: { turbo: false } },
              params: { continue: root_path } %>
          </div>
        </header>

        <!-- Flash Messages -->
        <div id="flash_messages" class="px-6 pt-4">
          <%= render "shared/flash" if flash.any? %>
        </div>

        <!-- Page Content -->
        <main class="flex-1 overflow-y-auto px-6 py-6">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
